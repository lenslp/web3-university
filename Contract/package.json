{
  "name": "web3-university-contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "copy-artifacts": "node scripts/copy-artifacts.js",
    "postbuild": "pnpm copy-artifacts",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost && pnpm copy-artifacts",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.ts --network sepolia && pnpm copy-artifacts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.10.6",
    "ethers": "^6.16.0",
    "hardhat": "^2.22.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
